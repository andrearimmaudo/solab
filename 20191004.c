#include "unistd.h"
#include "stdlib.h"
#include "stdio.h"
#include "unistd.h"

extern char **envinop //serve a passare le variabili d'ambiente del sistema operativo.

register int i; //register serve a salvare nell'area di memoria più veloce

int main(int argc, char const *argv[]) {

  return 0;
}

int name(/* arguments */) {
  static int count; //static permette di mantenere il valore da un'esecuzione all'altra della funzione
  return 0;
}
